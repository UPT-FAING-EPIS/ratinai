| Id | Caso de Prueba | Tipo de Prueba | Descripción | Área Funcional / Sub proceso | Funcionalidad / Característica | Requerimientos de Ambiente de Pruebas | Procedimientos especiales requeridos | Modalidad |
|----|----------------|----------------|-------------|------------------------------|--------------------------------|---------------------------------------|--------------------------------------|-----------|
| RF-01-UT01 | Generación de contraseña temporal válida de forma aislada | Unitaria | Script PHPUnit (`PasswordHelperTest.php`). Ejecuta 3 aserciones sobre `PasswordHelper::generateTemp(12)`: `assertEquals(12, strlen)` para longitud exacta, `assertTrue(ctype_alnum)` para el formato, y `assertNotEquals` al llamarla dos veces para confirmar unicidad del hash temporal. | Gestión de Accesos / Creación médica | Generación de contraseña temporal | PHP 8+, PHPUnit instalado vía Composer. Archivo PasswordHelper.php disponible. Sin base de datos. | Ejecutar comando `vendor/bin/phpunit tests/unit/PasswordHelperTest.php` aislado. | Automatizada |
| RF-01-FT01 | Registro exitoso de cuenta de oftalmólogo con envío de contraseña temporal | Funcional | Script Cypress (`create_doctor.cy.js`). Login de admin. Visita `create_doctor.php`, llena `#nombre`, `#correo` y `#cmp`. Selecciona el primer `.combo-opt` del `#combo-display` e inyecta valor estático en campo oculto `#password_override`. Valida redirect a `index.php` y presencia en el DOM de `.alert-flash.alert-ok`. | Gestión de Accesos / Creación médica | Crear cuenta de oftalmólogo | Servidor web corriendo en localhost/Azure. Cypress E2E configurado. Cuenta admin activa en BD. | Ejecutar `npx cypress run`. Limpiar cookies (`cy.clearCookies`) antes de iniciar. | Automatizada |
| RF-01-AT01 | Creación de cuenta de médico por Admin sin flujo de aprobación manual | Aceptación | Prueba Manual: 1) Login como Admin en `/views/auth/login.php`. 2) Menú Registrar Médico. 3) Ingreso de datos en formulario de la UI y clic en Guardar. 4) Aserción visual: La UI redirige al dashboard mostrando banner verde de éxito. 5) Se revisa la bandeja de entrada real para ver la clave temporal enviada por SMTP. | Gestión de Accesos / Creación médica | Crear cuenta de oftalmólogo | Navegador web, entorno Beta o Staging. Servicio de correo saliente SMTP habilitado. | Evidencia: Captura del banner de éxito UI y captura del correo SMTP recibido. | Manual |
| RF-02-UT01 | Verificación aislada del temporizador de expiración de sesión por inactividad | Unitaria | Script Jest (`session.service.test.js`). Usa `jest.useFakeTimers()`. 1) Inicializa con timeout 60000ms verificando disparo en `jest.advanceTimersByTime(60000)`. 2) Prueba `resetTimer()` reiniciando y avanzando tiempo. 3) Comprueba `destroy()` validando que la sesión quede nula. 4) Verifica default de 300000ms. | Seguridad / Gestión de sesión | Expiración de sesión por inactividad | Node.js instalado. Jest configurado. Archivo `assets/js/session.service.js` accesible. Sin BD. | Ejecutar `npm test`. Manipulación directa del reloj de JS usando fake timers. | Automatizada |
| RF-02-FT01 | Inicio de sesión con redirección obligatoria ante contraseña temporal | Funcional | Script Cypress (`login.cy.js`). 1) Visita Login y valida envíos vacíos. 2) Prueba clave errónea y valida `.alert-danger`. 3) Hook on-the-fly: hace login de admin por UI, crea médico `temp_login_${Date.now()}@...`, desloguea. 4) Login como temp; aserta obligatoria `cy.url().should('include', 'change_password.php')`. | Seguridad / Autenticación | Inicio y redirección por clave temporal | Entorno E2E. Cuenta admin existente para el script inyecte el usuario on-the-fly. | El script Cypress es autocontenido (crea la cuenta y luego la prueba en el mismo test). | Automatizada |
| RF-02-AT01 | Validación completa del flujo de autenticación y expiración de sesión por usuario final | Aceptación | Prueba Manual: 1) Entrar con clave temporal otorgada. 2) Comprobar UI de cambio de clave. 3) Forzar URL `/views/medico/dashboard.php` confirmando que devuelve a change_password. 4) Tras cambiar clave, inactividad física de 5 min exactos. Validar recarga de pantalla al Login por inactividad de JS. | Seguridad / Autenticación | Inicio y expiración de sesión | Navegador web estándar. Entorno Beta. Temporizador de muñeca físico (cronómetro). | Grabación de sesión limitando o probando URL protegidas en la barra de direcciones. | Manual |
| RF-03-UT01 | Generación aislada de nueva contraseña temporal en reseteo de cuenta médica | Unitaria | Script PHPUnit (`DoctorModelTest.php`). 1) Crea Mocks de `\PDO` y `\PDOStatement`. 2) Usa `ReflectionClass` inyectando Mock PDO en `$db` privada. 3) Aserción: Comprueba que el mock `prepare()` reciba string `UPDATE usuarios SET password=:pwd, es_password_temporal=1...` con bindings `:pwd`, `:id`, `:eid`. | Administración de médicos | Reseteo de contraseña de médico | PHP 8+, PHPUnit. Arquitectura limpia Mocks de capa persistencia. Sin DB real. | Inyectar Mocks usando la API Reflection de PHP para saltar constructor de DB. | Automatizada |
| RF-03-FT01 | Consulta, edición, reseteo y desactivación de médicos activos desde el panel Admin | Funcional | Script Cypress (`manage_doctors.cy.js`). Filtra filas UI por `cypress_doc_`. Clic `.btn-editar`, intercepta request como `@editDoctor`, espera JSON `body.success == true`. Prueba `window.confirm` para reset clave (`.btn-reset`). Clic `.btn-desactivar`, espera `@deactivateDoctor` y comprueba DOM "Inactivo". | Administración de médicos | Gestión de médicos activos | Entorno E2E. Cuentas creadas por `create_doctor.cy.js` con el prefijo "cypress_doc_" presentes. | El script debe usar filtros JQuery `:contains` en Cypress para no dañar médicos reales. | Automatizada |
| RF-03-AT01 | Gestión integral de médicos activos validada por el Admin en entorno representativo | Aceptación | Prueba Manual: Admin usa panel y barra de búsqueda para ubicar médico de prueba. Modifica en modal UI campo "Especialidad", confirmando que la fila muta usando AJAX sin refrescar F5 completo. Hace clic nativo en Desactivar, loguea fuera e intenta entrar con dicho médico recibiendo Access Denied. | Administración de médicos | Gestión de médicos activos | Entorno Beta. Mínimo un médico de prueba disponible. Cliente de correo para el reset. | Ejecutar aserciones de UI para feedback (Toast/Burbujas flotantes) al editar y borrar. | Manual |
| RF-04-UT01 | Validación aislada de campos obligatorios en el registro de establecimiento | Unitaria | Script PHPUnit (`EstablecimientoModelTest.php`). 1) Test `update()`: aserta SQL con bindings (?,?,?,?,?). 2) Test `getAdmin...()` aserta priorización de columna `id_usuario`. 3) Escenario Fallback: aserta búsqueda por `rol_codigo = 'ADM'` si id_usuario = null. 4) Test `getMedicos` por `rol_codigo='MED'`. | Gestión de establecimientos | Gestión interna de establecimientos | PHP 8+, PHPUnit. Arquitectura Mock de PDO y Statement. | Múltiples aserciones de invocaciones de query `expects($this->exactly(2))`. | Automatizada |
| RF-04-FT01 | Registro de establecimiento y asignación de Admin responsable por Super Admin | Funcional | Script Cypress combinado (`manage_establecimientos.cy.js` / `registro_centro.cy.js`). SuperAdmin intercepta POST a `EstablecimientoController`. Modifica `[name="ruc"]` comprobando error nativo HTML5 (`$input[0].validity.valid`). Intercepta simulación `action=send_code` de AJAX para evitar envío real de SMTP. | Gestión de establecimientos | Edición y Habilitación de establecimiento | Entorno E2E. Credencial Super Admin. Un establecimiento base id=1 creado por defecto en BD. | Interceptar Ajax de envío de códigos de correo para hacer unitaria funcional local. | Automatizada |
| RF-04-AT01 | Habilitación de establecimiento y autonomía operativa del Admin asignado en beta | Aceptación | Prueba Manual: Super Admin va a "Solicitudes de Registro", aprueba centro nuevo llenando RUC. Hace logout. Se ingresa sesión del nuevo Admin titular, validando que el panel está 100% aislado (vacío) mostrando solo los recursos locales de su establecimiento sin compartir filas con centro base. | Gestión de establecimientos | Habilitar establecimiento | Entorno Beta. Sesiones simultáneas o dos navegadores para Super Admin vs Admin Centro. | Navegación privada para descartar interferencia de cookies PHPPHPSESSID. | Manual |
| RF-05-UT01 | Validación aislada de formato de archivo de imagen retinal (JPG/PNG) | Unitaria | Script PHPUnit (`AnalisisControllerTest.php`). Inyecta Mock `AnalisisModel` vía `ReflectionClass`. Muta superglobales. Testea: Método GET falla asertando array JSON `error: 'Método no permitido'`. Falsifica `$_FILES['imagen']['type']='text/plain'` asertando error de Formato. Falsifica 11MB asertando tamaño max. | Procesamiento de imagen retinal | Validación de carga de análisis | PHP 8+, PHPUnit. Limpieza en `tearDown` de superglobales `$_SERVER`, `$_FILES`, `$_SESSION`. | Utilizar bufers `ob_start()` y `ob_get_clean()` para capturar prints JSON del MVC controller. | Automatizada |
| RF-05-FT01 | Carga de retinografía y obtención de resultado CNN en tiempo máximo de 5 segundos | Funcional | Script Cypress (`retinal_analysis.cy.js`). Login médico -> `#dni-input` -> click `#btn-buscar-paciente`. Ejecuta comando E2E `cy.get('input[type="file"]').selectFile('assets/images/retinopatia_normal.jpg')`. Submit -> Espera `#result-col` con `timeout:10000`. Extrae `%` de `#r_normal`, hace parseInt y aserta `> 50`. | Procesamiento de imagen retinal | Carga y análisis de imagen | Entorno E2E. Archivo JPG físico local en la ruta del test. API Flask remota viva. | Garantizar que el Backend Model IA devuelva los umbrales de diagnóstico en JSON real. | Automatizada |
| RF-05-AT01 | Flujo completo de análisis retinal validado por médico midiendo tiempo UI | Aceptación | Prueba Manual: Oftalmólogo logueado anexa JPG real usando UI nativa OS (Finder/Explorador). Comprueba la barra de progreso de Spinner o Loader visual. Mide con reloj físico <5s de bloqueo. Verifica en UI barras de probabilidad semánticas visuales (Verde=Normal, Rojo=Anomalía) confirmando UX médico clínico. | Procesamiento de imagen retinal | Carga y análisis de imagen | Entorno Productivo/Beta. Experiencia del usuario real (médico especializado). | Recopilar Feedback o "Think out loud" protocol del oftalmólogo leyendo UI de diagnóstico. | Manual |
| RF-06-UT01 | Generación aislada de códigos únicos de historial sin colisiones (Pendiente) | Unitaria | Script PHPUnit (planeado `PatientModelTest.php`). 1) Simulación en bucle de la BD respondiendo 'existe_codigo'=1 para forzar la iteración while() en `generateHistoryCode()` y probar resiliencia del `Uuid::uuid4()`. 2) Prueba que `getHistory` retorne array PHP ordenado DESC por fecha del Mock. | Historial de paciente | Generación y vinculación de historial | PHP 8+, PHPUnit. Mocks de `\PDOStatement` retornando conteos de SQL falsos (colisiones). | Construir data mock JSON con diagnósticos antiguos y nuevos intercalados. | Automatizada |
| RF-06-FT01 | Consulta cronológica automatizada en panel de historial UI (Pendiente) | Funcional | Script Cypress (planeado `history_patient.cy.js`). Ingresa `#hist-dni`. Usa `cy.intercept('GET', '**/action=history')` devolviendo mock JSON de 2 diagnósticos ficticios. Usa `cy.get('.history-card')` validando `.should('have.length', 2)` y aserta DOM extrayendo fechas texto para validar cronología visual (reciente arriba). | Historial de paciente | Consulta de historial | Entorno E2E de Cypress. Sin necesidad de BD poblada (uso intensivo de Mocking API local). | Empleo de Cypress alias `@getHistory` para pausar el script hasta el pintado DOM. | Automatizada |
| RF-06-AT01 | Verificación del historial de paciente con código único en flujo real UI (Pendiente)| Aceptación | Prueba Manual: Navegación libre "exploratoria". Médico da clic en menú, abre el historial de un paciente de alta frecuencia (3+ análisis reales en BD). Expande el acordeón HTML visualmente. Comprueba consistencia del campo UUID del historial vs URL, y valida las miniaturas de imágenes procesadas. | Historial de paciente | Consulta de historial | Entorno Beta. Data relacional de producción sanitizada con históricos abundantes. | Ninguno, test heurístico general del usuario final sobre UX. | Manual |
| RF-07-UT01 | Validación aislada algorítmica de Regex del DNI en Controller (Pendiente) | Unitaria | Script PHPUnit (planeado `PatientControllerTest.php`). Testea Request Sanitization aislando métodos de búsqueda. Falsifica variables `$_POST['dni'] = '123A4567'`, asertando de inmediato `success: false, error: 'Formato'`. Prueba `$_POST` vacío y strings SQLInjection comprobando bloqueos lógicos antes de SQL. | Búsqueda de pacientes | Búsqueda por DNI | Entorno Local PHPUnit. | Ejecutar sin carga completa del Bootstrap PHP para probar solo clases base puras. | Automatizada |
| RF-07-FT01 | Recuperación visual y asíncrona por DNI válido e inválido en Panel (Pendiente) | Funcional | Script Cypress (planeado `search_patient.cy.js`). Digita DNI erróneo, valida intercept 404/Error, comprueba `.alert-warning` en pantalla "Paciente no encontrado" y NO recarga la página. Digita DNI válido mockeado JSON, aserta que contenedor de resultados quita clase "d-none" mostrando `#patient-name`. | Búsqueda de pacientes | Búsqueda por DNI | Entorno E2E Cypress. | Forzar tiempos de timeout en Mock `delay: 1500` para poder validar el Loading State Spinner del DOM. | Automatizada |
| RF-07-AT01 | Búsqueda exploratoria manual guiada por errores UI del médico (Pendiente) | Aceptación | Prueba Manual: Digitar intencionalmente textos e inyecciones SQL en UI de búsqueda confirmando error HTML5. Comprobar posición del Tooltip/Toast del error asíncrono para paciente inexistente, midiendo nivel de frustración o amigabilidad UX. Encontrar un paciente y avanzar flujo normal de UI. | Búsqueda de pacientes | Búsqueda por DNI | Navegador de uso general. | Testing Exploratorio libre buscando romper la UI de FrontEnd por ingresos maliciosos. | Manual |
| RF-08-UT01 | Verificación mapeo de Constantes y Configuración del Modelo IA (Pendiente) | Unitaria | Script PHPUnit (planeado `ModelConfigTest.php`). Carga la clase contenedora de métricas y aserta vía Reflection o getters públicos (`$config->getAccuracy()`) que los números coinciden al bit con el diccionario duro u origen DB, confirmando parser del ConfigParser/DB row extraction. | Panel del modelo | Exposición de métricas | Entorno local PHPUnit. Archivo JSON/INI o Mock BD configurado. | Aislar el Singleton de Configuración limpiando instancias estáticas de PHP. | Automatizada |
| RF-08-FT01 | Rendering del panel informativo y dominios textuales del DOM (Pendiente) | Funcional | Script Cypress (planeado). Navega a ruta `ModelInfo`. Usa selectores `.model-spec`, `.accuracy-val` para asegurar en Cypress `should('contain.text', 'ODIR-5K')` garantizando que los metadatos y limitaciones se renderizan visualmente sin truncar texto CSS o variables no parseadas de PHP ({{}} erróneos). | Panel del modelo | Panel informativo | Entorno E2E Frontend. | Verificar en vista Móvil y Desktop (Viewport resizing cy.viewport). | Automatizada |
| RF-08-AT01 | Nivel de lectura y comprensibilidad clínica del texto explicativo (Pendiente) | Aceptación | Prueba Manual Clínica: Un Oftalmólogo lee el texto desplegado sobre "Sensibilidad" y "Especificidad" en la pantalla de "Sobre la IA". Emite un juicio cualitativo sobre si los disclaimers protegen legalmente a la herramienta y son suficientes para su marco regulatorio clínico interno (UX Content Strategy). | Panel del modelo | Panel informativo | Entorno Beta/Mockup de Alta Fidelidad. | Entrevista corta de 2 minutos sobre Usabilidad Textual al médico de turno. | Manual |
| RNF-01-AT01 | Aserción Hard-Latency del servicio /predict de Flask (Pendiente) | Aceptación | Script Cypress Avanzado (planeado). Intercepta POST a /predict. Guarda Date.now() a inicio. Al resolverse promesa, aserta `expect(t2-t1).to.be.lte(5000)`. Repite bloque `it()` un bucle de N=10 subidas con un fixture `.selectFile()` verificando que ni el Average ni el Maximo rompan barrera de 5000ms SLA. | Procesamiento de imagen retinal | Rendimiento de latencia | Cypress local vs Instancia Flask Remota Production-like Server. | Desactivar logs pesados en Flask backend para reflejar performance nativo C++ de PyTorch/ONNX. | Automatizada |
| RNF-02-AT01 | Heurística de la Regla de los 3 clics en Flujo Completo (Pendiente) | Aceptación | Prueba Manual UI Audit: Tester externo ejecuta sesión grabando pantalla. Clic 1 (Login submit) -> Clic 2 (Botón de Buscar Tras teclear DNI) -> Clic 3 (Botón Analizar tras select local). Evalúa que el resultado emerge visualmente sin enviar al médico por laberintos de menús u otras ventanas modales. | Procesamiento de imagen retinal | Usabilidad del flujo | Entorno de Producción. Sin Cypress para medir carga humana cognitiva real. | Contabilización estricta por Video-Replay de las pantallas y clicks invertidos. | Manual |
| RNF-03-AT01 | Aserción y Desacoplamiento de PII (Info Identificable) en Tablas SQL (Pendiente) | Aceptación | Prueba Técnica Data Engineering: El DBA o tester inspecciona DBeaver conectando DB remota. Realiza `SELECT * FROM analyses`. Inspecciona columnas JSON / BLOB y el `response` de Flask en tabla. Confirma que Flask solo responde con `img_hash` o UUID, nunca PII (Nombres, Telf). Confirma FK separada a `patients`. | Privacidad y Seguridad | Anonimización de datos | DBeaver, TablePlus o consola CLI MySQL/Postgres remota. | Auditoría directa a nivel de Motor de Base de Datos y Payload de Red con F12 (DevTools). | Manual |
| RNF-04-AT01 | Ejecución de Pipelines cruzados Chromium/Firefox (Pendiente) | Aceptación | Script Cypress CI (GitHub Actions/Bash). Ejecuta proceso `npx cypress run --browser chrome` y luego `npx cypress run --browser firefox`. Aserción: Ambos test runners devuelven `exit code 0` confirmando compatibilidad CSS/JS sin sintaxis que rompa Gecko vs Blink. Manualmente se verifica alineamiento CSS. | General del sistema | Compatibilidad web | Runner CI (Linux/Mac) con librerías nativas de navegadores para Headless GUI E2E instalados. | Añadir banderas strict en el runner Cypress de Github. | Automatizada |
| RNF-05-AT01 | Script de Stress Multiconcurrente JMeter/K6 p(95) < 2s (Pendiente) | Aceptación | Script K6 (planeado `stress_test.js`). 100 Virtual Users ejecutando request `http.post` de Logins simultáneos durante 60 segundos. Aserción de métricas K6: `http_req_duration: ['p(95)<2000']`, 95% peticiones HTTP bajo 2 segundos. Y cero (0%) drop de request TCP/SQL 'too many connections'. | General del sistema | Rendimiento y concurrencia | Herramienta CLI de K6 instalada, base de datos con limites max_connections MySQL ampliado. | Lanzar script fuera de horas laborales si es Server Live, o en Sandbox privado. | Automatizada |
